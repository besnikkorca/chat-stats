export enum TextSize {
  xlarge = 'xlarge',
  large = 'large',
  medium = 'medium',
  small = 'small',
}

export enum TextColor {
  primary = 'primary',
  secondary = 'secondary',
  ternary = 'ternary',
}

export enum TextAlign {
  left = 'left',
  right = 'right',
  center = 'center',
}
