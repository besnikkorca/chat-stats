export enum TextSize {
  xlarge = 'xlarge',
  large = 'large',
  medium = 'medium',
  small = 'small',
  xsmall = 'xsmall',
}

export enum TextColor {
  primary = 'primary',
  secondary = 'secondary',
  ternary = 'ternary',
}

export enum TextAlign {
  left = 'left',
  right = 'right',
  center = 'center',
}
